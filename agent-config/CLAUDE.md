# Personal Claude Code instructions

Add cross-conversation guidance here. Knowledge files have moved to `skills/`
so they only load when relevant — don't re-add `@`-imports here unless the
rule must apply to every conversation.
